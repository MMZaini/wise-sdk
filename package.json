{
  "name": "wise-sdk-workspace",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "spec:update": "node scripts/fetch-spec.mjs",
    "spec:check": "node scripts/check-spec.mjs",
    "generate": "node scripts/generate.mjs"
  },
  "devDependencies": { "fern-api": "5.120.1", "yaml": "2.9.1" }
}
