# This file was auto-generated by Fern from our API Definition.

import typing

from .. import core
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..kyc_review.types.kyc_review import KycReview
from ..kyc_review.types.kyc_review_with_link import KycReviewWithLink
from .raw_client import AsyncRawKycReviewsClient, RawKycReviewsClient
from .types.create_kyc_reviews_request_trigger_data import CreateKycReviewsRequestTriggerData
from .types.create_kyc_reviews_request_type import CreateKycReviewsRequestType
from .types.submit_requirement_kyc_reviews_request_requirement_key import (
    SubmitRequirementKycReviewsRequestRequirementKey,
)

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class KycReviewsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawKycReviewsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawKycReviewsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawKycReviewsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        profile_id: int,
        page_number: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[KycReview]:
        """
        Retrieves a list of all active KYC Reviews for a given profile.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        page_number : typing.Optional[int]
            Page number. Default value is `1`.

        page_size : typing.Optional[int]
            Desired number of items per page. Max `100`, default `100`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[KycReview]
            Returns a list of KYC Review objects for the profile. Returns an empty list if no KYC Review exists for the profile.

        Examples
        --------
        from wise_sdk.generated import WiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = WiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )
        client.kyc_reviews.list(
            profile_id=1000000,
        )
        """
        _response = self._raw_client.list(
            profile_id=profile_id, page_number=page_number, page_size=page_size, request_options=request_options
        )
        return _response.data

    def create(
        self,
        *,
        profile_id: int,
        type: CreateKycReviewsRequestType,
        trigger_data: typing.Optional[CreateKycReviewsRequestTriggerData] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> KycReview:
        """
        Creates a KYC Review for a specific customer action.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        type : CreateKycReviewsRequestType
            Type of the underlying action or process this KYC Review is for. Usually a reference to which product this KYC Review is for (like `QUOTE` or `TRANSFER`) or a reference to a KYC process on the profile that isn't related to a specific product (like `REFRESH_CYCLE` or `REPAPERING`).

            Following types are available when creating a KYC Review using this endpoint:
            - `QUOTE`
            - `PROACTIVE_SEND_MONEY`
            - `CARD`

        trigger_data : typing.Optional[CreateKycReviewsRequestTriggerData]
            Object containing data of the underlying product object that triggered the KYC Review.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReview
            KYC Review successfully created.

        Examples
        --------
        from wise_sdk.generated import WiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = WiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )
        client.kyc_reviews.create(
            profile_id=1000000,
            type="QUOTE",
        )
        """
        _response = self._raw_client.create(
            profile_id=profile_id, type=type, trigger_data=trigger_data, request_options=request_options
        )
        return _response.data

    def get(
        self, *, profile_id: int, kyc_review_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> KycReview:
        """
        Retrieves a single KYC Review by ID for a profile.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        kyc_review_id : str
            The KYC Review ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReview
            Returns the KYC Review object including the `requirements` field.

        Examples
        --------
        from wise_sdk.generated import WiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = WiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )
        client.kyc_reviews.get(
            profile_id=1000000,
            kyc_review_id="kycReviewId",
        )
        """
        _response = self._raw_client.get(
            profile_id=profile_id, kyc_review_id=kyc_review_id, request_options=request_options
        )
        return _response.data

    def update_redirect_url(
        self,
        *,
        profile_id: int,
        kyc_review_id: str,
        redirect_url: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> KycReviewWithLink:
        """
        Updates the KYC Review with a redirect URL.

        Returns the KYC Review object with a `link` field containing a URL where the end customer needs to be directed in order to complete the Hosted KYC flow. **The link is short-lived and single-use and valid for 5 minutes**. Use the `expiresAt` field in the response to determine the exact expiry time. To regenerate the link, call this endpoint again.

        Once the Hosted KYC flow is completed by the end customer, they will be redirected to the `redirectUrl` provided in this API call. During the redirection, the `redirectUrl` will be appended with query parameters: `status=success`, `status=failed`, or `status=closed`.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        kyc_review_id : str
            The KYC Review ID.

        redirect_url : str
            URL where the user will be redirected at the end of the flow. Can contain query params and path fragments. It has to be a valid URL as per [RFC 2396](https://www.ietf.org/rfc/rfc2396.txt).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReviewWithLink
            Returns the updated KYC Review object.

        Examples
        --------
        from wise_sdk.generated import WiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = WiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )
        client.kyc_reviews.update_redirect_url(
            profile_id=1000000,
            kyc_review_id="kycReviewId",
            redirect_url="https://example.com",
        )
        """
        _response = self._raw_client.update_redirect_url(
            profile_id=profile_id,
            kyc_review_id=kyc_review_id,
            redirect_url=redirect_url,
            request_options=request_options,
        )
        return _response.data

    def submit_requirement(
        self,
        *,
        profile_id: int,
        requirement_key: SubmitRequirementKycReviewsRequestRequirementKey,
        submission_data: str,
        files: typing.Optional[typing.List[core.File]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Submit a KYC requirement for a KYC Review.

        - A KYC requirement can only be submitted if the KYC Review status is `WAITING_CUSTOMER_INPUT` or `PASSED_WITH_REQUIREMENTS`.
        - A KYC requirement should only be submitted if its `state` is `NOT_PROVIDED` and `apiCollectionSupported` is `true`.

        See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for the full list of supported requirement types, submission data structures, and accepted values.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        requirement_key : SubmitRequirementKycReviewsRequestRequirementKey
            The KYC requirement type to submit.

            See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for details on each type.

        submission_data : str
            A JSON string containing the following fields:

            - `data` — an object whose fields vary by requirement type. Some requirement types do not require a `data` field.
            - `version` — the submission version, currently `"V1"`.
            - `kycReviewId` — the ID of the KYC Review this submission belongs to.

            For example, the JSON payload would look like below, but it should be included as a string value in the multipart form field:

            ```json
            {
              "data": {
                "accountPurpose": "SENDING_MONEY_TO_FRIENDS_OR_FAMILY"
              },
              "version": "V1",
              "kycReviewId": "93a903ea-636c-4c86-a25c-cf6d2bd00e81"
            }
            ```

            See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for the `data` structure and accepted values for each requirement type.

        files : typing.Optional[typing.List[core.File]]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from wise_sdk.generated import WiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = WiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )
        client.kyc_reviews.submit_requirement(
            profile_id=1000000,
            requirement_key="ACCOUNT_INTENT",
            submission_data="submissionData",
        )
        """
        _response = self._raw_client.submit_requirement(
            profile_id=profile_id,
            requirement_key=requirement_key,
            submission_data=submission_data,
            files=files,
            request_options=request_options,
        )
        return _response.data


class AsyncKycReviewsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawKycReviewsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawKycReviewsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawKycReviewsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        profile_id: int,
        page_number: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[KycReview]:
        """
        Retrieves a list of all active KYC Reviews for a given profile.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        page_number : typing.Optional[int]
            Page number. Default value is `1`.

        page_size : typing.Optional[int]
            Desired number of items per page. Max `100`, default `100`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[KycReview]
            Returns a list of KYC Review objects for the profile. Returns an empty list if no KYC Review exists for the profile.

        Examples
        --------
        import asyncio

        from wise_sdk.generated import AsyncWiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = AsyncWiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )


        async def main() -> None:
            await client.kyc_reviews.list(
                profile_id=1000000,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            profile_id=profile_id, page_number=page_number, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        *,
        profile_id: int,
        type: CreateKycReviewsRequestType,
        trigger_data: typing.Optional[CreateKycReviewsRequestTriggerData] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> KycReview:
        """
        Creates a KYC Review for a specific customer action.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        type : CreateKycReviewsRequestType
            Type of the underlying action or process this KYC Review is for. Usually a reference to which product this KYC Review is for (like `QUOTE` or `TRANSFER`) or a reference to a KYC process on the profile that isn't related to a specific product (like `REFRESH_CYCLE` or `REPAPERING`).

            Following types are available when creating a KYC Review using this endpoint:
            - `QUOTE`
            - `PROACTIVE_SEND_MONEY`
            - `CARD`

        trigger_data : typing.Optional[CreateKycReviewsRequestTriggerData]
            Object containing data of the underlying product object that triggered the KYC Review.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReview
            KYC Review successfully created.

        Examples
        --------
        import asyncio

        from wise_sdk.generated import AsyncWiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = AsyncWiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )


        async def main() -> None:
            await client.kyc_reviews.create(
                profile_id=1000000,
                type="QUOTE",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            profile_id=profile_id, type=type, trigger_data=trigger_data, request_options=request_options
        )
        return _response.data

    async def get(
        self, *, profile_id: int, kyc_review_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> KycReview:
        """
        Retrieves a single KYC Review by ID for a profile.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        kyc_review_id : str
            The KYC Review ID.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReview
            Returns the KYC Review object including the `requirements` field.

        Examples
        --------
        import asyncio

        from wise_sdk.generated import AsyncWiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = AsyncWiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )


        async def main() -> None:
            await client.kyc_reviews.get(
                profile_id=1000000,
                kyc_review_id="kycReviewId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(
            profile_id=profile_id, kyc_review_id=kyc_review_id, request_options=request_options
        )
        return _response.data

    async def update_redirect_url(
        self,
        *,
        profile_id: int,
        kyc_review_id: str,
        redirect_url: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> KycReviewWithLink:
        """
        Updates the KYC Review with a redirect URL.

        Returns the KYC Review object with a `link` field containing a URL where the end customer needs to be directed in order to complete the Hosted KYC flow. **The link is short-lived and single-use and valid for 5 minutes**. Use the `expiresAt` field in the response to determine the exact expiry time. To regenerate the link, call this endpoint again.

        Once the Hosted KYC flow is completed by the end customer, they will be redirected to the `redirectUrl` provided in this API call. During the redirection, the `redirectUrl` will be appended with query parameters: `status=success`, `status=failed`, or `status=closed`.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        kyc_review_id : str
            The KYC Review ID.

        redirect_url : str
            URL where the user will be redirected at the end of the flow. Can contain query params and path fragments. It has to be a valid URL as per [RFC 2396](https://www.ietf.org/rfc/rfc2396.txt).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        KycReviewWithLink
            Returns the updated KYC Review object.

        Examples
        --------
        import asyncio

        from wise_sdk.generated import AsyncWiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = AsyncWiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )


        async def main() -> None:
            await client.kyc_reviews.update_redirect_url(
                profile_id=1000000,
                kyc_review_id="kycReviewId",
                redirect_url="https://example.com",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_redirect_url(
            profile_id=profile_id,
            kyc_review_id=kyc_review_id,
            redirect_url=redirect_url,
            request_options=request_options,
        )
        return _response.data

    async def submit_requirement(
        self,
        *,
        profile_id: int,
        requirement_key: SubmitRequirementKycReviewsRequestRequirementKey,
        submission_data: str,
        files: typing.Optional[typing.List[core.File]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Submit a KYC requirement for a KYC Review.

        - A KYC requirement can only be submitted if the KYC Review status is `WAITING_CUSTOMER_INPUT` or `PASSED_WITH_REQUIREMENTS`.
        - A KYC requirement should only be submitted if its `state` is `NOT_PROVIDED` and `apiCollectionSupported` is `true`.

        See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for the full list of supported requirement types, submission data structures, and accepted values.

        Parameters
        ----------
        profile_id : int
            The profile ID.

        requirement_key : SubmitRequirementKycReviewsRequestRequirementKey
            The KYC requirement type to submit.

            See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for details on each type.

        submission_data : str
            A JSON string containing the following fields:

            - `data` — an object whose fields vary by requirement type. Some requirement types do not require a `data` field.
            - `version` — the submission version, currently `"V1"`.
            - `kycReviewId` — the ID of the KYC Review this submission belongs to.

            For example, the JSON payload would look like below, but it should be included as a string value in the multipart form field:

            ```json
            {
              "data": {
                "accountPurpose": "SENDING_MONEY_TO_FRIENDS_OR_FAMILY"
              },
              "version": "V1",
              "kycReviewId": "93a903ea-636c-4c86-a25c-cf6d2bd00e81"
            }
            ```

            See the [KYC requirement types](/guides/product/kyc/wise-kyc/kyc-requirement-types) guide for the `data` structure and accepted values for each requirement type.

        files : typing.Optional[typing.List[core.File]]
            See core.File for more documentation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from wise_sdk.generated import AsyncWiseClient
        from wise_sdk.generated.environment import WiseClientEnvironment

        client = AsyncWiseClient(
            external_correlation_id="YOUR_EXTERNAL_CORRELATION_ID",
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
            access_token="YOUR_ACCESS_TOKEN",
            environment=WiseClientEnvironment.SANDBOX,
        )


        async def main() -> None:
            await client.kyc_reviews.submit_requirement(
                profile_id=1000000,
                requirement_key="ACCOUNT_INTENT",
                submission_data="submissionData",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_requirement(
            profile_id=profile_id,
            requirement_key=requirement_key,
            submission_data=submission_data,
            files=files,
            request_options=request_options,
        )
        return _response.data
