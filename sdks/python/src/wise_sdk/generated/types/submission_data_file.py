# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class SubmissionDataFile(UniversalBaseModel):
    """
    Submission data for FILE_SUBMISSION message type.
    Used to attach previously uploaded files to a case.

    **Prerequisites:**
    - Files must be uploaded via `POST /cases/{caseId}/files` before submitting
    - Case must not be in RESOLVED or FAILED status
    - Files must belong to the specified profile
    - Files must have been uploaded by the same partner

    **Validation Rules:**
    - `profileId` is REQUIRED
    - `fileIds` is REQUIRED and must not be empty
    - `fileIds` must not contain duplicates
    - All file IDs must reference existing files
    - All files must belong to the specified `profileId`
    - All files must have been created by the submitting partner
    """

    profile_id: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="profileId"),
        pydantic.Field(
            alias="profileId",
            description="The profile ID that the files belong to.\nMust match the profile associated with the uploaded files.",
        ),
    ]
    """
    The profile ID that the files belong to.
    Must match the profile associated with the uploaded files.
    """

    file_ids: typing_extensions.Annotated[
        typing.List[str],
        FieldMetadata(alias="fileIds"),
        pydantic.Field(
            alias="fileIds",
            description="List of file IDs to attach to the case.\nFiles must have been previously uploaded via the file upload endpoint.",
        ),
    ]
    """
    List of file IDs to attach to the case.
    Files must have been previously uploaded via the file upload endpoint.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
