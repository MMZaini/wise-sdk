// This file was auto-generated by Fern from our API Definition.

import type * as Wise from "../index.js";

/**
 * A support case with its complete details including messages and references.
 */
export interface Case2 {
    /** Unique identifier of the case. */
    id: number | bigint;
    /** The profile ID associated with this case. */
    profileId: number | bigint;
    /** The type of the support case. */
    type: Case2.Type;
    /** The sub-type of the support case. */
    subType?: Case2.SubType | undefined;
    /** The current status of the support case. */
    status: Case2.Status;
    /** Partner-created identifier for this case, if applicable. */
    externalId?: string | undefined;
    /**
     * Timestamp when the case was created.
     * Format: ISO 8601 datetime without timezone (YYYY-MM-DDTHH:mm:ss.SSS).
     */
    createdAt: string;
    /**
     * Timestamp when the case was last updated.
     * Format: ISO 8601 datetime without timezone (YYYY-MM-DDTHH:mm:ss.SSS).
     */
    updatedAt?: string | undefined;
    /** Reference information linking the case to a related entity. */
    reference?: Case2.Reference | undefined;
    /** List of messages associated with this case. */
    messages: Wise.CaseMessage[];
}

export namespace Case2 {
    /** The type of the support case. */
    export const Type = {
        Sanctions: "SANCTIONS",
        GeneralEnquiry: "GENERAL_ENQUIRY",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
    /** The sub-type of the support case. */
    export const SubType = {
        RecipientLocationHit: "RECIPIENT_LOCATION_HIT",
        ReferenceLocationHit: "REFERENCE_LOCATION_HIT",
        RecipientSanctionHit: "RECIPIENT_SANCTION_HIT",
        ReferenceSanctionHit: "REFERENCE_SANCTION_HIT",
        DepositSanctionHit: "DEPOSIT_SANCTION_HIT",
    } as const;
    export type SubType = (typeof SubType)[keyof typeof SubType];
    /** The current status of the support case. */
    export const Status = {
        Creating: "CREATING",
        InReview: "IN_REVIEW",
        WaitingCustomerInput: "WAITING_CUSTOMER_INPUT",
        Resolved: "RESOLVED",
        Failed: "FAILED",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];

    /**
     * Reference information linking the case to a related entity.
     */
    export interface Reference {
        type: Reference.Type;
        referenceId: string;
    }

    export namespace Reference {
        export const Type = {
            Transfer: "TRANSFER",
        } as const;
        export type Type = (typeof Type)[keyof typeof Type];
    }
}
