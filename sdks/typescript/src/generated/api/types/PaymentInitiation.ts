// This file was auto-generated by Fern from our API Definition.

/**
 * Payment initiation for funding a batch group via direct debit.
 */
export interface PaymentInitiation {
    /** Payment initiation ID. */
    id?: (number | bigint) | undefined;
    /** Batch group ID. */
    batchGroupId?: string | undefined;
    /** Payment reference that will be passed to the network. Can be used for reconciliation. */
    reference?: string | undefined;
    /** The ID of the user who initiated this payment. */
    userId?: (number | bigint) | undefined;
    /** The ID of the profile this payment belongs to. */
    profileId?: (number | bigint) | undefined;
    /** Payment type. */
    type?: PaymentInitiation.Type | undefined;
    /**
     * Payment initiation status:
     * - `NEW` — Payment initiation created
     * - `PROCESSING` — Payment is being processed
     * - `COMPLETED` — Payment completed successfully
     * - `FAILED` — Payment failed
     * - `CHARGED_BACK` — Payment was charged back
     */
    status?: PaymentInitiation.Status | undefined;
    /** External account ID associated with the payment. */
    accountId?: (number | bigint) | undefined;
    /** Transfer ID of the direct debit payment. Present only after the direct debit is initiated. */
    transferId?: ((number | bigint) | null) | undefined;
    /** Date and time when the payment initiation was created. */
    createdTime?: string | undefined;
}

export namespace PaymentInitiation {
    /** Payment type. */
    export const Type = {
        DirectDebit: "DIRECT_DEBIT",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
    /**
     * Payment initiation status:
     * - `NEW` — Payment initiation created
     * - `PROCESSING` — Payment is being processed
     * - `COMPLETED` — Payment completed successfully
     * - `FAILED` — Payment failed
     * - `CHARGED_BACK` — Payment was charged back
     */
    export const Status = {
        New: "NEW",
        Processing: "PROCESSING",
        Completed: "COMPLETED",
        Failed: "FAILED",
        ChargedBack: "CHARGED_BACK",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
