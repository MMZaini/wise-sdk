// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../BaseClient.js";
import { type NormalizedClientOptions, normalizeClientOptions } from "../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers.js";
import * as core from "../../../../core/index.js";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError.js";
import * as errors from "../../../../errors/index.js";
import * as Wise from "../../../index.js";

export declare namespace ComparisonsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class ComparisonsClient {
    protected readonly _options: NormalizedClientOptions<ComparisonsClient.Options>;

    constructor(options: ComparisonsClient.Options) {
        this._options = normalizeClientOptions(options);
    }

    /**
     * Request price and speed comparison data for money transfer providers on a given currency route.
     *
     * You must provide either `sendAmount` or `recipientGetsAmount` (but not both).
     *
     * @param {Wise.GetComparisonsRequest} request
     * @param {ComparisonsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Wise.TooManyRequestsError}
     * @throws {@link errors.WiseError}
     * @throws {@link errors.WiseTimeoutError}
     *
     * @example
     *     await client.comparisons.get({
     *         sourceCurrency: "GBP",
     *         targetCurrency: "EUR"
     *     })
     */
    public get(
        request: Wise.GetComparisonsRequest,
        requestOptions?: ComparisonsClient.RequestOptions,
    ): core.HttpResponsePromise<Wise.Comparison> {
        return core.HttpResponsePromise.fromPromise(this.__get(request, requestOptions));
    }

    private async __get(
        request: Wise.GetComparisonsRequest,
        requestOptions?: ComparisonsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Wise.Comparison>> {
        const {
            sourceCurrency,
            targetCurrency,
            sendAmount,
            recipientGetsAmount,
            midMarketRate,
            wiseFee,
            wiseEstimatedDelivery,
            wiseQuoteCreatedTime,
            payInMethod,
            sourceCountry,
            targetCountry,
            providerCountry,
            filter,
            numberOfProviders,
            providerTypes,
            providers,
            excludePartners,
            includeWise,
        } = request;
        const _queryParams: Record<string, unknown> = {
            sourceCurrency,
            targetCurrency,
            sendAmount,
            recipientGetsAmount,
            midMarketRate,
            wiseFee,
            wiseEstimatedDelivery,
            wiseQuoteCreatedTime,
            payInMethod,
            sourceCountry,
            targetCountry,
            providerCountry,
            filter: filter != null ? filter : undefined,
            numberOfProviders,
            providerTypes: Array.isArray(providerTypes)
                ? providerTypes.map((item) => item)
                : providerTypes != null
                  ? providerTypes
                  : undefined,
            providers,
            excludePartners,
            includeWise,
        };
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-External-Correlation-Id":
                    requestOptions?.externalCorrelationId ?? this._options?.externalCorrelationId,
            }),
            requestOptions?.headers,
        );
        const _response = await (this._options.fetcher ?? core.fetcher)({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)).api,
                "comparisons",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Wise.Comparison, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 429:
                    throw new Wise.TooManyRequestsError(
                        _response.error.body as Record<string, unknown>,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.WiseError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/comparisons");
    }
}
