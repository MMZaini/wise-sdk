// This file was auto-generated by Fern from our API Definition.

export interface CardOrder {
    /** ID of the card order. */
    id?: (number | bigint) | undefined;
    /** Profile ID. */
    profileId?: (number | bigint) | undefined;
    /** Client ID. */
    clientId?: string | undefined;
    /** The card program associated with this card order. A Card Program is what Wise refers to all the cards that you will be issuing with us, grouped by product type and by issuing country. */
    cardProgram?: CardOrder.CardProgram | undefined;
    /** Address set during card order. **Required** for physical cards. **Optional** for virtual cards (defaults to the profile address, if omitted). Fields vary by country. See the [card address validation guide](/guides/developer/api-guides/card-address-validation) for country-specific fields and validation rules. */
    address?: CardOrder.Address | undefined;
    /** Token of the card associated with card order. Nullable. */
    cardToken?: (string | null) | undefined;
    /** Token of the card being replaced by this replacement card order. Nullable. */
    replacesCard?: (string | null) | undefined;
    /** Time when the card order is created. */
    creationTime?: string | undefined;
    /** Time when the card order was last modified. */
    modificationTime?: string | undefined;
    /** Status of the card order. See [card order status flow](/api-reference/card-order#card-order-status-flow) for details. */
    status?: CardOrder.Status | undefined;
    /** Name of the card holder. */
    cardHolderName?: string | undefined;
    /** Phone number associated with the card order. */
    phoneNumber?: string | undefined;
    /** Maximum amount of spending on the card once issued. Nullable. */
    lifetimeLimit?: (number | null) | undefined;
    /**
     * The estimated time when the card will be delivered. There are few scenarios to be mindful of:
     *
     * 1. For virtual card the delivery estimate will be close to the creationTime, as it does not require delivery.
     * 2. For physical card in `PLACED` status, the delivery estimate is calculated assuming that the order requirements will be fulfilled today (refreshed daily).
     * 3. For physical card after `PLACED` status, we provide a best effort estimation, and it should not be used as delivery timing as we will have separate delivery tracking (subject to region availability) for physical card that is coming soon.
     */
    deliveryEstimate?: string | undefined;
    /** Delivery details of a physical card order. For virtual cards, this value is null. */
    deliveryDetails?: CardOrder.DeliveryDetails | undefined;
}

export namespace CardOrder {
    /**
     * The card program associated with this card order. A Card Program is what Wise refers to all the cards that you will be issuing with us, grouped by product type and by issuing country.
     */
    export interface CardProgram {
        /** The name of the card program. */
        name?: string | undefined;
        /**
         * The network of the card program:
         * - `MASTERCARD`
         * - `VISA`
         */
        scheme?: CardProgram.Scheme | undefined;
        /** The default currency assigned to the card program. */
        defaultCurrency?: string | undefined;
        /**
         * The type of the card:
         * - `VIRTUAL_NON_UPGRADEABLE`
         * - `PHYSICAL`
         */
        cardType?: CardProgram.CardType | undefined;
    }

    export namespace CardProgram {
        /**
         * The network of the card program:
         * - `MASTERCARD`
         * - `VISA`
         */
        export const Scheme = {
            Mastercard: "MASTERCARD",
            Visa: "VISA",
        } as const;
        export type Scheme = (typeof Scheme)[keyof typeof Scheme];
        /**
         * The type of the card:
         * - `VIRTUAL_NON_UPGRADEABLE`
         * - `PHYSICAL`
         */
        export const CardType = {
            VirtualNonUpgradeable: "VIRTUAL_NON_UPGRADEABLE",
            Physical: "PHYSICAL",
        } as const;
        export type CardType = (typeof CardType)[keyof typeof CardType];
    }

    /**
     * Address set during card order. **Required** for physical cards. **Optional** for virtual cards (defaults to the profile address, if omitted). Fields vary by country. See the [card address validation guide](/guides/developer/api-guides/card-address-validation) for country-specific fields and validation rules.
     */
    export interface Address {
        /** Card holder's address. */
        firstLine?: string | undefined;
        /** Card holder's address. */
        secondLine?: (string | null) | undefined;
        /** Card holder's address. */
        thirdLine?: (string | null) | undefined;
        /** Card holder's city. */
        city?: string | undefined;
        /** Card holder's postal code. */
        postCode?: string | undefined;
        /** Card holder's state. */
        state?: (string | null) | undefined;
        /** Card holder's country (ISO 3166-1 alpha-2). */
        country?: string | undefined;
        /** Accepts any additional properties */
        [key: string]: any;
    }

    /** Status of the card order. See [card order status flow](/api-reference/card-order#card-order-status-flow) for details. */
    export const Status = {
        Placed: "PLACED",
        RequirementsFulfilled: "REQUIREMENTS_FULFILLED",
        CardDetailsCreated: "CARD_DETAILS_CREATED",
        Produced: "PRODUCED",
        Completed: "COMPLETED",
        Cancelled: "CANCELLED",
        Returned: "RETURNED",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];

    /**
     * Delivery details of a physical card order. For virtual cards, this value is null.
     */
    export interface DeliveryDetails {
        /** The delivery option used on the card order. */
        deliveryOption?: DeliveryDetails.DeliveryOption | undefined;
        /** The name of the delivery vendor. */
        deliveryVendor?: (string | null) | undefined;
        /** The URL to track the card delivery. */
        trackingUrl?: (string | null) | undefined;
        /** The tracking number of the card delivery. */
        trackingNumber?: (string | null) | undefined;
    }

    export namespace DeliveryDetails {
        /** The delivery option used on the card order. */
        export const DeliveryOption = {
            PostalServiceStandard: "POSTAL_SERVICE_STANDARD",
            PostalServiceWithTracking: "POSTAL_SERVICE_WITH_TRACKING",
            KioskCollection: "KIOSK_COLLECTION",
        } as const;
        export type DeliveryOption = (typeof DeliveryOption)[keyof typeof DeliveryOption];
    }
}
