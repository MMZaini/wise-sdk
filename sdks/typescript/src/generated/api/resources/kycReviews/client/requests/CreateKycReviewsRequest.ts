// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         profileId: BigInt("1000000"),
 *         type: "QUOTE"
 *     }
 */
export interface CreateKycReviewsRequest {
    /** The profile ID. */
    profileId: number | bigint;
    /**
     * Type of the underlying action or process this KYC Review is for. Usually a reference to which product this KYC Review is for (like `QUOTE` or `TRANSFER`) or a reference to a KYC process on the profile that isn't related to a specific product (like `REFRESH_CYCLE` or `REPAPERING`).
     *
     * Following types are available when creating a KYC Review using this endpoint:
     * - `QUOTE`
     * - `PROACTIVE_SEND_MONEY`
     * - `CARD`
     */
    type: CreateKycReviewsRequest.Type;
    /** Object containing data of the underlying product object that triggered the KYC Review. */
    triggerData?: CreateKycReviewsRequest.TriggerData;
}

export namespace CreateKycReviewsRequest {
    /**
     * Type of the underlying action or process this KYC Review is for. Usually a reference to which product this KYC Review is for (like `QUOTE` or `TRANSFER`) or a reference to a KYC process on the profile that isn't related to a specific product (like `REFRESH_CYCLE` or `REPAPERING`).
     *
     * Following types are available when creating a KYC Review using this endpoint:
     * - `QUOTE`
     * - `PROACTIVE_SEND_MONEY`
     * - `CARD`
     */
    export const Type = {
        Quote: "QUOTE",
        ProactiveSendMoney: "PROACTIVE_SEND_MONEY",
        Card: "CARD",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];

    /**
     * Object containing data of the underlying product object that triggered the KYC Review.
     */
    export interface TriggerData {
        /** ID of the underlying product object. For example, if type is `QUOTE` then this would be the quote ID. This ID might be null when referencing an ID isn't meaningful. As an example, there's only ever one active refresh cycle per profile so referencing by ID isn't useful. */
        id?: string | undefined;
    }
}
