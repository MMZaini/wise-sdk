// This file was auto-generated by Fern from our API Definition.

import type * as Wise from "../../../../index.js";

/**
 * @example
 *     {
 *         profileId: BigInt("1000000"),
 *         scheme: "MASTERCARD",
 *         reason: "WRONG_AMOUNT",
 *         transaction: {
 *             transactionId: "12345",
 *             email: "support@partner.com"
 *         },
 *         form: {
 *             "charged": {
 *                 "value": 50,
 *                 "currency": "EUR"
 *             },
 *             "expected": {
 *                 "value": 5,
 *                 "currency": "EUR"
 *             },
 *             "agreedDetails": {
 *                 "agreed": true,
 *                 "agreedReason": "some reason"
 *             },
 *             "goods": "something",
 *             "details": "wrong amount dispute"
 *         },
 *         disclaimer: {
 *             allAnswered: true,
 *             filesUploaded: true
 *         }
 *     }
 */
export interface SubmitDisputesRequest {
    /** Your profile ID. */
    profileId: number | bigint;
    /** The network of the card that was used to make this transaction. */
    scheme: Wise.SubmitDisputesRequestScheme;
    /** Dispute reason code supplied by the [dispute reasons endpoint](/api-reference/disputes/disputereasonsget). */
    reason: string;
    /** Transaction details for the dispute. */
    transaction?: SubmitDisputesRequest.Transaction;
    /**
     * Form fields required to submit this dispute. The specific fields vary per reason code.
     *
     * See the [disputes via API guide](/guides/product/issue-cards/card-disputes-api#reason-examples) for per-reason field details.
     */
    form?: Record<string, unknown>;
    /** Acknowledgement fields. */
    disclaimer?: SubmitDisputesRequest.Disclaimer;
    /** Key-value pairs mapping file field names to file IDs obtained from the [file upload endpoint](/api-reference/disputes/disputefileupload). Available file fields vary per reason code. */
    files?: Record<string, string>;
}

export namespace SubmitDisputesRequest {
    /**
     * Transaction details for the dispute.
     */
    export interface Transaction {
        /** ID of the transaction. */
        transactionId?: string | undefined;
        /** Email of the support team that Wise should reach out to. */
        email?: string | undefined;
    }

    /**
     * Acknowledgement fields.
     */
    export interface Disclaimer {
        /** Acknowledgment that all questions have been answered. Must be `true` or the request will be rejected. */
        allAnswered?: boolean | undefined;
        /** Acknowledgment that all relevant files have been attached. Must be `true` or the request will be rejected. */
        filesUploaded?: boolean | undefined;
    }
}
