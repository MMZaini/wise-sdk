// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         profileId: BigInt("1000000")
 *     }
 */
export interface CreateDirectDebitAccountsRequest {
    /** Your profile ID. */
    profileId: number | bigint;
    /** Currency of the bank account. 3-character currency code — `USD` for ACH direct debits, `CAD` for EFT direct debits. */
    currency?: string;
    /**
     * Payment type for which the account is used.
     *
     * - `ACH` — US direct debit (USD)
     * - `EFT` — CA direct debit (CAD)
     */
    type?: CreateDirectDebitAccountsRequest.Type;
    /** Bank account details. */
    details?: CreateDirectDebitAccountsRequest.Details;
}

export namespace CreateDirectDebitAccountsRequest {
    /**
     * Payment type for which the account is used.
     *
     * - `ACH` — US direct debit (USD)
     * - `EFT` — CA direct debit (CAD)
     */
    export const Type = {
        Ach: "ACH",
        Eft: "EFT",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];

    /**
     * Bank account details.
     */
    export interface Details {
        /** A valid ABA routing number. For CAD direct debits, the routing number is a combination of an institution number + transit number. */
        routingNumber?: string | undefined;
        /** A valid bank account number. */
        accountNumber?: string | undefined;
        /**
         * Bank account type.
         *
         * - `CHECKING` — Checking account (USD, CAD)
         * - `SAVINGS` — Savings account (USD, CAD)
         */
        accountType?: Details.AccountType | undefined;
    }

    export namespace Details {
        /**
         * Bank account type.
         *
         * - `CHECKING` — Checking account (USD, CAD)
         * - `SAVINGS` — Savings account (USD, CAD)
         */
        export const AccountType = {
            Checking: "CHECKING",
            Savings: "SAVINGS",
        } as const;
        export type AccountType = (typeof AccountType)[keyof typeof AccountType];
    }
}
