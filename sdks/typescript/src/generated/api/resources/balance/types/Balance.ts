// This file was auto-generated by Fern from our API Definition.

import type * as Wise from "../../../index.js";

/**
 * Represents a balance account within a profile.
 */
export interface Balance {
    /** Balance ID. */
    id?: (number | bigint) | undefined;
    /** Currency code (ISO 4217 Alphabetic Code). */
    currency?: string | undefined;
    type?: Wise.BalanceType | undefined;
    /** Name of the balance. Required for SAVINGS balances. */
    name?: (string | null) | undefined;
    /** Icon for the balance. */
    icon?: (Balance.Icon | null) | undefined;
    /**
     * Investment state of the balance.
     * - `NOT_INVESTED` - Balance is not invested.
     * - `INVESTED` - Balance is invested in assets.
     * - `INVESTING` - Balance is being invested into assets.
     * - `DIVESTING` - Balance is being divested from assets.
     * - `UNKNOWN` - Investment state is unknown.
     */
    investmentState?: Balance.InvestmentState | undefined;
    /** Available balance that can be used to fund transfers. */
    amount?: Balance.Amount | undefined;
    /** Amount reserved for transactions. */
    reservedAmount?: Balance.ReservedAmount | undefined;
    /** Cash amount in the account. */
    cashAmount?: Balance.CashAmount | undefined;
    /** Current total worth. */
    totalWorth?: Balance.TotalWorth | undefined;
    /** Date when the balance was created. */
    creationTime?: string | undefined;
    /** Date when the balance was last modified. */
    modificationTime?: string | undefined;
    /** Whether the balance is visible to the user. */
    visible?: boolean | undefined;
}

export namespace Balance {
    /**
     * Icon for the balance.
     */
    export interface Icon {
        /** Icon type. */
        type?: Icon.Type | undefined;
        /** Icon value (e.g., emoji character). */
        value?: string | undefined;
    }

    export namespace Icon {
        /** Icon type. */
        export const Type = {
            Emoji: "EMOJI",
        } as const;
        export type Type = (typeof Type)[keyof typeof Type];
    }

    /**
     * Investment state of the balance.
     * - `NOT_INVESTED` - Balance is not invested.
     * - `INVESTED` - Balance is invested in assets.
     * - `INVESTING` - Balance is being invested into assets.
     * - `DIVESTING` - Balance is being divested from assets.
     * - `UNKNOWN` - Investment state is unknown.
     */
    export const InvestmentState = {
        NotInvested: "NOT_INVESTED",
        Invested: "INVESTED",
        Investing: "INVESTING",
        Divesting: "DIVESTING",
        Unknown: "UNKNOWN",
    } as const;
    export type InvestmentState = (typeof InvestmentState)[keyof typeof InvestmentState];

    /**
     * Available balance that can be used to fund transfers.
     */
    export interface Amount {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Amount reserved for transactions.
     */
    export interface ReservedAmount {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Cash amount in the account.
     */
    export interface CashAmount {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Current total worth.
     */
    export interface TotalWorth {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }
}
