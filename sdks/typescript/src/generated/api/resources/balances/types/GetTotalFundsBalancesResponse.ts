// This file was auto-generated by Fern from our API Definition.

/**
 * Total funds overview for a profile.
 */
export interface GetTotalFundsBalancesResponse {
    /** Total worth of the account, including cash ledger balance and valuation of any asset portfolio if invested. */
    totalWorth?: GetTotalFundsBalancesResponse.TotalWorth | undefined;
    /** Total available balance, which is the sum of cash ledger balance and any approved overdraft limit. */
    totalAvailable?: GetTotalFundsBalancesResponse.TotalAvailable | undefined;
    /** Total cash balance across all balances, including group balances but excluding asset portfolios. */
    totalCash?: GetTotalFundsBalancesResponse.TotalCash | undefined;
    /** Overdraft details for the account. */
    overdraft?: GetTotalFundsBalancesResponse.Overdraft | undefined;
}

export namespace GetTotalFundsBalancesResponse {
    /**
     * Total worth of the account, including cash ledger balance and valuation of any asset portfolio if invested.
     */
    export interface TotalWorth {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Total available balance, which is the sum of cash ledger balance and any approved overdraft limit.
     */
    export interface TotalAvailable {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Total cash balance across all balances, including group balances but excluding asset portfolios.
     */
    export interface TotalCash {
        /** Amount value. */
        value?: number | undefined;
        /** Currency code (ISO 4217 Alphabetic Code). */
        currency?: string | undefined;
    }

    /**
     * Overdraft details for the account.
     */
    export interface Overdraft {
        /** Maximum overdraft available through an overdraft program. Zero if no approved overdraft. */
        limit?: Overdraft.Limit | undefined;
        /** Portion of the approved overdraft limit currently being utilized. */
        used?: Overdraft.Used | undefined;
        /** Amount of overdraft currently available (limit minus used). */
        available?: Overdraft.Available | undefined;
        /** Available overdraft amounts converted to each currency the customer has a balance in. */
        availableByCurrency?: Overdraft.AvailableByCurrency.Item[] | undefined;
    }

    export namespace Overdraft {
        /**
         * Maximum overdraft available through an overdraft program. Zero if no approved overdraft.
         */
        export interface Limit {
            /** Amount value. */
            value?: number | undefined;
            /** Currency code (ISO 4217 Alphabetic Code). */
            currency?: string | undefined;
        }

        /**
         * Portion of the approved overdraft limit currently being utilized.
         */
        export interface Used {
            /** Amount value. */
            value?: number | undefined;
            /** Currency code (ISO 4217 Alphabetic Code). */
            currency?: string | undefined;
        }

        /**
         * Amount of overdraft currently available (limit minus used).
         */
        export interface Available {
            /** Amount value. */
            value?: number | undefined;
            /** Currency code (ISO 4217 Alphabetic Code). */
            currency?: string | undefined;
        }

        export type AvailableByCurrency = AvailableByCurrency.Item[];

        export namespace AvailableByCurrency {
            export interface Item {
                /** Amount value. */
                value?: number | undefined;
                /** Currency code (ISO 4217 Alphabetic Code). */
                currency?: string | undefined;
            }
        }
    }
}
