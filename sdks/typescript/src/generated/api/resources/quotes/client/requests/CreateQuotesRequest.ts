// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         profileId: BigInt("1000000"),
 *         sourceCurrency: "GBP",
 *         targetCurrency: "USD"
 *     }
 */
export interface CreateQuotesRequest {
    /** Personal or business profile ID of the sender. */
    profileId: number | bigint;
    /** Source (sending) currency code. */
    sourceCurrency: string;
    /** Target (receiving) currency code. */
    targetCurrency: string;
    /** Amount in source currency. Either `sourceAmount` or `targetAmount` is required, never both. */
    sourceAmount?: number | null;
    /** Amount in target currency. */
    targetAmount?: number | null;
    /** _Optional_. This is the ID of transfer recipient, found in the create recipient response. If provided, can be used as an alternative to [updating the quote](/api-reference/quote/quoteupdate). */
    targetAccount?: number | bigint;
    /**
     * _Optional_. Preferred payout method.
     *
     * Default value is `BANK_TRANSFER`.
     *
     * Examples of other accepted values include `BALANCE`, `SWIFT`, `SWIFT_OUR`, `ALIPAY`, and `INTERAC`.
     */
    payOut?: string | null;
    /** _Optional_. Preferred payin method. Use `BANK_TRANSFER` to return this method at the top of the response's results. */
    preferredPayIn?: string | null;
    /** _Optional_. Used to pass additional metadata about the intended transfer. */
    paymentMetadata?: CreateQuotesRequest.PaymentMetadata;
    /** Required when configured for your client ID. Includes a `pricingConfiguration` to be used for pricing calculations with the quote. */
    pricingConfiguration?: CreateQuotesRequest.PricingConfiguration;
}

export namespace CreateQuotesRequest {
    /**
     * _Optional_. Used to pass additional metadata about the intended transfer.
     */
    export interface PaymentMetadata {
        /** _Optional_. Used to pass transfer nature for calculating proper tax amounts (IOF) for transfers to and from BRL. Accepted values are shown dynamically in transfer requirements. */
        transferNature?: string | undefined;
    }

    /**
     * Required when configured for your client ID. Includes a `pricingConfiguration` to be used for pricing calculations with the quote.
     */
    export interface PricingConfiguration {
        fee?: PricingConfiguration.Fee | undefined;
    }

    export namespace PricingConfiguration {
        export interface Fee {
            /** Identifies the type of fee to be configured. Options include only `OVERRIDE`. */
            type?: string | undefined;
            /** The selected variable percentage, in decimal format, that should be used in the `pricingConfiguration`. */
            variable?: number | undefined;
            /** The selected fixed fee that should be used in the `pricingConfiguration`. Always considered in source currency. */
            fixed?: number | undefined;
        }
    }
}
